# Shopping List App

A simple shopping list built with:

- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Local state with `localStorage` persistence

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Deploy to Vercel

This project is Vercel-ready with zero extra configuration.
